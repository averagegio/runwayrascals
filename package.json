{
  "name": "runwayrascals",
  "private": true,
  "description": "Runway Rascals — static game + Vercel serverless API",
  "scripts": {
    "start:api": "npm start --prefix server",
    "install:all": "npm install && npm install --prefix server"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.23.0",
    "stripe": "^16.12.0"
  },
  "engines": {
    "node": ">=18"
  }
}
